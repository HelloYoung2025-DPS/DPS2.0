# .omo 2.0 — Git 排除规则
# 瞬态状态（不提交）
sessions/*.json
sessions/archive/
locks/
changes/
validations/

# 持久化状态（提交）
# !config.json        — 系统配置
# !contracts/*.json   — 接口合约
# !modules/*.json     — 模块状态
# !PROJECT.md, MODULES.md, context.md, decisions.md, conventions.md
