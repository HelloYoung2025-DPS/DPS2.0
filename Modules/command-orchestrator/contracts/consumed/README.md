# Consumed contracts

- `operation.compiled/v1`, owned by `operation-compiler`.
- `command.receipt/v1`, self-owned protocol consumed from its real producer (`executor-gateway`) before exact relay to Audit Metrics.
