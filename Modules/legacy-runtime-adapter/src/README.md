# legacy-runtime-adapter source

Legacy source stays in the temporary owned paths declared by ../module.yaml until each responsibility is extracted and verified. Do not copy the whole legacy runtime here.
