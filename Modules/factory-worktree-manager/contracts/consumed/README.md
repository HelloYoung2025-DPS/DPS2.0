# Consumed contracts

- `instruction.receipt/v1` must be `BOUND`, current, and cover every affected module.
- `module.change.plan/v1` supplies trusted-policy roles, owned paths, dependencies, and contract-worktree requirements.

Unknown majors and producer mismatches fail closed.
